package fr.bloctave.lmr.api.effortlessbuilding

import fr.bloctave.lmr.config.util.AreaConfigImpl

class EffortlessBuildingConfig : AreaConfigImpl() {

    /*var blockPlace: Boolean
    var blockBreak: Boolean


    init {
        blockPlace = false
        blockBreak = false
    }

    override fun bake() {
        blockPlace = COMMON.blockPlace.get()
        blockBreak = COMMON.blockBreak.get()
    }


    class EffortlessBuildingConfigBuilder(builder: ForgeConfigSpec.Builder): ConfigBuilder() {
        val blockPlace: ForgeConfigSpec.BooleanValue = builder
            .comment("If true area owners can toggle whether passive entities can spawn in the area")
            .define("passiveSpawning", false)
        val blockBreak: ForgeConfigSpec.BooleanValue = builder
            .comment("If true area owners can toggle whether hostile entities can spawn in the area")
            .define("hostileSpawning", false)
    }*/

}